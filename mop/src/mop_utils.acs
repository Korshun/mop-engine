function int TeamPlayerCount(int team)
{
	int count = 0;
	for (int player = 0; player < MAX_PLAYERS; player++)
		if (PlayerInGame(player) && GetPlayerInfo(player, PLAYERINFO_TEAM) == team)
			count++;
	return count;
}
